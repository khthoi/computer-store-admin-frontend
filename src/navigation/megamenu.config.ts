import type { SidebarMenuCategory } from "@/src/components/navigation/MegaMenu";

// Stub — client storefront megamenu config (not used in admin dashboard)
export const STORE_MEGA_MENU: SidebarMenuCategory[] = [];
