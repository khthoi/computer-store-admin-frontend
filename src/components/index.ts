/**
 * Component Library — root barrel
 *
 * Usage:
 *   import { Button, Input, ProductCard, DataTable } from "@/src/components";
 */

// GROUP 1: Base UI — complete
export * from "./ui";

// GROUP 2: Navigation — complete
export * from "./navigation";

// GROUP 3: Admin — complete
export * from "./admin";

