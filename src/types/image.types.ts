// ─── Image domain types ────────────────────────────────────────────────────

export interface HinhAnhSanPham {
  id: string;
  variantId: string;
  url: string;
  alt: string;
  displayOrder: number;
  createdAt: string; // ISO date string
}
